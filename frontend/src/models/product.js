class Product {
  constructor(id, name, description, countInStock, imageUrl, price) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.countInStock = countInStock;
    this.imageUrl = imageUrl;
    this.price = price;
  }
}

export default Product;
